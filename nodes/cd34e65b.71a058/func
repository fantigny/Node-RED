var payload = msg.payload;

var used = 100 * payload[2].capacity;
used = used.toFixed(0);

var free = 100 - used;
free = free.toFixed(0);

msg.payload = [{
    "series": [ "used", "free" ],
    "data": [ [used, free] ],
    "labels": [ "used", "free" ]
}];

return msg;