var payload = msg.payload;

var free = 100 * (payload.freemem / payload.totalmem);
free = free.toFixed(0);

var used = 100 - free;
used = used.toFixed(0);

msg.payload = [{
    "series": [ "used", "free" ],
    "data": [ [ used, free ] ],
    "labels": [ "used", "free" ]
}];

return msg;