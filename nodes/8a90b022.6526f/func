
var celsius = msg.payload.celsius;
if (celsius !== undefined) {
    node.log('[Hue] temperature (' + celsius + '°C)');
    return;
}

var motion = msg.payload.motion;
if (motion !== undefined) {
    node.log('[Hue] motion');
    return;
}

var lux = msg.payload.lux;
if (lux !== undefined) {
    node.log('[Hue] brightness (' + lux + ' lux)');
    return;
}