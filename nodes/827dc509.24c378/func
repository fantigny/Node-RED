var home = msg.payload.body.homes[0];
flow.set('homeId', home.id);
flow.set('lastEventId', home.events[0].id);

//global.set('vpnUrl', home.cameras[0].vpn_url + '/live/index_local.m3u8');

node.log('[Welcome] at ' + home.name);

return msg;