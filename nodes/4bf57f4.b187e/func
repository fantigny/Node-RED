
var celsius = msg.payload.celsius;
if (celsius !== undefined) {
    var adjusted_celsius = msg.payload.adjusted_celsius;
    node.log('[Hue] temperature(' +
        celsius + ') adjusted to ' +
        adjusted_celsius + ' °C');
}

var motion = msg.payload.motion;
if (motion !== undefined) {
    node.log('[Hue] motion');
}

var lux = msg.payload.lux;
if (lux !== undefined) {
    var adjusted_lux = msg.payload.adjusted_lux;
    node.log('[Hue] brightness(' +
        lux + ') adjusted to ' +
        adjusted_lux + ' lux');
}